let user = {};
user.name = "John";
user.surname = "Smith";
user.name = "Pete";
delete user.name;

//Function that checks for emptiness
function isEmpty(obj){
    for (let key in obj){
        return false;
    }
    return true;
}

//Sum of object properties
let sum = 0;
for (let key in salaries){
    sum += salaries[key];
}
alert(sum)
    
//Multiply numeric property values by 2
function multiplyNumeric(obj){
    for (let key in obj){
        if (typeof obj[key] == 'number'){
            obj[key] *= 2;
        }
    }  
        
    }
